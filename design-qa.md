# ChatExcel Design QA

## 范围

- 用户提供的参考截图仅用于本机视觉对照，不纳入公开仓库。
- 当前任务窗格：`assets/screenshots/taskpane-400x900.png`
- 当前设置页：`assets/screenshots/settings-400x900.png`
- 验收日期：2026-08-01
- 验收环境：Codex in-app browser、本地 HTTPS `https://localhost:3210/taskpane.html?preview=1`

## 对比结果

| 目标 | 实现结果 | 状态 |
| --- | --- | --- |
| 模型、思考等级、上下文和审批模式位于输入框底部 | 控件固定在输入区工具栏，320px 宽度仍无横向溢出 | 通过 |
| 操作记录在上、对话在下 | 操作记录固定在对话上方，行高紧凑且可折叠 | 通过 |
| 一次任务的操作记录分组 | 预览页默认只显示一个任务组；组头显示任务名、步骤预览、步数和状态，展开后显示全部步骤 | 通过 |
| 多协议流式文字 | 四种协议的 SSE 增量、普通 JSON 回退和最终结果归一化由自动化测试覆盖；任务窗格只接收脱敏 `delta` 事件 | 通过 |
| 运行中发送/停止状态 | 同一个发送控件运行时显示旋转圆环，悬停或键盘聚焦切换为停止图标；不存在独立停止按钮 | 通过 |
| 右上角齿轮打开配置页 | 齿轮按钮可键盘聚焦，设置页打开后背景区域设为 `inert` | 通过 |
| API 协议和根地址分离 | 下拉框提供四种协议，根地址提示自动补 `/v1`、`/v1beta` 和方法路径 | 通过 |
| 最大步骤数放入设置页 | 默认 100，输入范围 1-1000；保存、关闭重开和服务重启后均恢复 | 通过 |
| 输入框默认一行并自动增高 | 初始 34px，三行文本增至 65px，最大高度保持受控 | 通过 |
| 粘贴图片进入附件队列 | 剪贴板 PNG 生成预览和删除按钮，删除后发送按钮恢复禁用 | 通过 |
| 毛玻璃 UI 和克制动效 | 半透明表面、细边框；设置页、附件、活动记录和状态反馈使用 180ms 左右过渡 | 通过 |
| 页脚彩蛋动效 | `ChatEx` 页脚固定 28px；悬停/键盘聚焦显现 CSS 基线场景，点击固定活动状态，`prefers-reduced-motion` 下保持静态 | 通过 |

## 响应式证据

- `400x900`：`body.scrollWidth = 400`，任务窗格宽度为 400，无横向滚动。
- `320x700`：`body.scrollWidth = 320`，composer 宽度 310，发送按钮完整显示。
- `320x700` 设置页：表单 `scrollWidth = clientWidth = 305`，最大步骤数和底部操作按钮均可见。
- 页脚彩蛋：400px 和 320px 宽度均无横向溢出；入口拥有 `aria-label` 和 `aria-pressed`，活动层使用 `aria-hidden`。
- 所有动效在 `prefers-reduced-motion: reduce` 下缩短到 `0.01ms` 且只执行一次。

## 流式与任务组证据

- `assets/screenshots/taskpane-400x900.png` 是重载后的默认状态截图：任务组折叠，组头保留 `整理 Sheet1 的验收数据`、三个步骤名预览、`3 步` 和 `完成`；对话位于其下方。
- 预览页点击任务组头后可展开三个步骤，再次点击可折叠；每个步骤仍是可聚焦按钮，历史上下文选择逻辑不变。
- `tests/provider-stream.test.js` 覆盖 OpenAI Responses、Chat Completions、Anthropic Messages、Gemini 的文本增量、usage 归一化、工具参数拼接和 Responses `item_id`/`call_id` 关联；`tests/http-app.test.js` 覆盖本地 `delta/result/done` 事件。
- 真实桌面 Excel 中连接可控 SSE 提供方的长文本和中途停止尚未完成，因此不把浏览器预览证据描述为桌面端到端流式验收。

## 验收中修复

1. 上下文长度默认值 `200000` 与旧 `step="1024"` 不匹配，导致浏览器原生表单校验阻止保存。已改为 `step="1"`，服务端仍校验 1024-2000000 的整数范围。
2. 设置页打开后背景控件仍出现在辅助技术树中。已为背景区域增加 `inert` 和 `aria-hidden`，关闭设置页时恢复。

## 截图

![任务窗格](assets/screenshots/taskpane-400x900.png)

![设置页](assets/screenshots/settings-400x900.png)

## Microsoft Excel 桌面冒烟验收

- `npm run sideload` 成功注册清单并打开隔离测试工作簿，功能区显示 `ChatEx / 打开 ChatExcel`，任务窗格可正常加载。
- Excel 内设置页显示“最大步骤数”“默认 100”，当前输入值为 `100`。
- 工作簿信息与 `A1:C4` 读取成功；`A1:C4` 写值、`D1` 写值和 `D2` 写公式均经逐项审批成功，`=SUM(C2:C4)` 的实际结果为 `60`。
- 当前自定义模型随后把 `format_range.bold` 发送为错误类型，服务按严格 Schema 拒绝且未执行该格式修改。格式、表格和图表的最终端到端回归仍保持未完成，不以模型参数错误替代验收。

## 对话流布局增量验收

- 验收日期：2026-08-02
- 验收环境：Codex in-app browser、本地 HTTPS `https://localhost:3210/taskpane.html?preview=1`
- 预览数据：一个用户消息、两个连续自动审批动作和一个模型完成消息；顶部操作记录继续作为可展开的审计入口。

| 目标 | 实现结果 | 状态 |
| --- | --- | --- |
| 模型与用户消息按角色分列 | 模型气泡贴左、用户气泡贴右；400px 下两者宽度均为 247px，320px 下均为 194px，约为对话可用宽度的三分之二 | 通过 |
| 大圆角消息气泡 | 模型和用户消息使用 18px 圆角，文本自动换行 | 通过 |
| 连续动作居中展示 | 两个自动审批动作被渲染为同一个居中 `action-flow`，每一步使用椭圆胶囊 | 通过 |
| 动作方向连续 | 相邻动作之间显示一个向下箭头；用户、模型或错误消息会结束动作组 | 通过 |
| 窄窗格稳定性 | 400px 与 320px 下均为 `body.scrollWidth = body.clientWidth`，没有横向溢出或控件遮挡 | 通过 |
| 浏览器运行状态 | 当前页面控制台无 error 日志 | 通过 |

![400px 对话流](assets/screenshots/conversation-flow-400x900.png)

![320px 对话流](assets/screenshots/conversation-flow-320x700.png)

## 结论

浏览器任务窗格视觉、响应式布局、默认折叠任务组、左右对话气泡、居中连续动作流、单按钮运行/停止状态、最大步骤数持久化、协议下拉、剪贴板图片和输入增高均达到当前设计验收标准。四种协议的流式服务链路已通过自动化测试；Microsoft Excel 已通过加载、配置、读取、写值和公式冒烟验证，但桌面端长文本流式和中途停止、格式/表格/图表完整回归仍未完成。

final result: passed
