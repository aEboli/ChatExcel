# ChatExcel Design QA

## 范围

- 用户提供的参考截图仅用于本机视觉对照，不纳入公开仓库。
- 当前任务窗格：`assets/screenshots/taskpane-400x900.png`
- 当前设置页：`assets/screenshots/settings-400x900.png`
- 验收日期：2026-08-01
- 验收环境：Codex in-app browser、本地 HTTPS `https://localhost:3210/taskpane.html?preview=1`

## 对比结果

| 目标 | 实现结果 | 状态 |
| --- | --- | --- |
| 模型、思考等级、上下文和审批模式位于输入框底部 | 控件固定在输入区工具栏，320px 宽度仍无横向溢出 | 通过 |
| 操作记录在上、对话在下 | 操作记录固定在对话上方，行高紧凑且可折叠 | 通过 |
| 右上角齿轮打开配置页 | 齿轮按钮可键盘聚焦，设置页打开后背景区域设为 `inert` | 通过 |
| API 协议和根地址分离 | 下拉框提供四种协议，根地址提示自动补 `/v1`、`/v1beta` 和方法路径 | 通过 |
| 最大步骤数放入设置页 | 默认 100，输入范围 1-1000；保存、关闭重开和服务重启后均恢复 | 通过 |
| 输入框默认一行并自动增高 | 初始 34px，三行文本增至 65px，最大高度保持受控 | 通过 |
| 粘贴图片进入附件队列 | 剪贴板 PNG 生成预览和删除按钮，删除后发送按钮恢复禁用 | 通过 |
| 毛玻璃 UI 和克制动效 | 半透明表面、细边框；设置页、附件、活动记录和状态反馈使用 180ms 左右过渡 | 通过 |

## 响应式证据

- `400x900`：`body.scrollWidth = 400`，任务窗格宽度为 400，无横向滚动。
- `320x700`：`body.scrollWidth = 320`，composer 宽度 310，发送按钮完整显示。
- `320x700` 设置页：表单 `scrollWidth = clientWidth = 305`，最大步骤数和底部操作按钮均可见。
- 所有动效在 `prefers-reduced-motion: reduce` 下缩短到 `0.01ms` 且只执行一次。

## 验收中修复

1. 上下文长度默认值 `200000` 与旧 `step="1024"` 不匹配，导致浏览器原生表单校验阻止保存。已改为 `step="1"`，服务端仍校验 1024-2000000 的整数范围。
2. 设置页打开后背景控件仍出现在辅助技术树中。已为背景区域增加 `inert` 和 `aria-hidden`，关闭设置页时恢复。

## 截图

![任务窗格](assets/screenshots/taskpane-400x900.png)

![设置页](assets/screenshots/settings-400x900.png)

## Microsoft Excel 桌面冒烟验收

- `npm run sideload` 成功注册清单并打开隔离测试工作簿，功能区显示 `ChatEx / 打开 ChatExcel`，任务窗格可正常加载。
- Excel 内设置页显示“最大步骤数”“默认 100”，当前输入值为 `100`。
- 工作簿信息与 `A1:C4` 读取成功；`A1:C4` 写值、`D1` 写值和 `D2` 写公式均经逐项审批成功，`=SUM(C2:C4)` 的实际结果为 `60`。
- 当前自定义模型随后把 `format_range.bold` 发送为错误类型，服务按严格 Schema 拒绝且未执行该格式修改。格式、表格和图表的最终端到端回归仍保持未完成，不以模型参数错误替代验收。

## 结论

浏览器任务窗格视觉、响应式布局、最大步骤数持久化、协议下拉、剪贴板图片和输入增高均达到当前设计验收标准。Microsoft Excel 已通过加载、配置、读取、写值和公式冒烟验证；格式、表格和图表的完整回归仍未完成，不影响本次浏览器设计验收结论。

final result: passed
