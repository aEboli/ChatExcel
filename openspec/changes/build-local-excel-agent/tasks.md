## 1. 项目骨架与依赖

- [x] 1.1 创建 Node.js ESM 项目结构、共享模块目录、Excel 清单、图标生成脚本和基础 npm 命令；运行 `npm run check` 验证所有 JavaScript 语法。
- [x] 1.2 安装锁定的运行时与开发依赖，运行 `npm audit --omit=dev` 并记录未解决风险。

## 2. 本机配置与安全服务

- [x] 2.1 实现结构化 TOML 配置解析、提供方选择、Responses URL 和令牌解析；用 Node 测试覆盖成功、缺失凭据和非 Responses 场景。
- [x] 2.2 实现回环地址 HTTPS 服务、Host/Origin/JSON 限制、健康接口和脱敏配置接口；用 HTTP 集成测试验证拒绝未授权来源和不泄露令牌。

## 3. Responses Agent 编排

- [x] 3.1 实现 Responses 客户端、错误脱敏、超时和配置逐步重读；用模拟 HTTP 提供方测试请求字段与异常路径。
- [x] 3.2 实现内存会话、完整输出项续传、工具结果校验、可配置步骤上限、取消和过期清理；用单元测试覆盖正常循环、未知工具和步骤超限。
- [x] 3.3 对当前 `localhost:8080/responses` 发送最小只读请求，验证 `store: false`、严格函数工具及工具结果续传兼容性，并按实测同步适配层和设计工件。

## 4. Excel 工具层

- [x] 4.1 建立共享严格工具 Schema、中文标签和读取/修改分类；用测试确保名称唯一、Schema 可序列化且修改工具完整分类。
- [x] 4.2 实现 Office.js 读取工具及范围大小限制；在 Excel 中验证工作簿信息、当前选区和指定范围返回值与公式。
- [x] 4.3 实现值、公式、格式、自动调整、清除、工作表、表格、图表和排序工具；在隔离测试区域逐项验证原生对象和结果。

## 5. 任务窗格与交互

- [x] 5.1 实现简体中文任务窗格、配置状态、会话消息、工具审计记录、输入发送和停止状态；在窄任务窗格与桌面宽度下检查无溢出。
- [x] 5.2 实现前端 Agent 循环、读取工具自动执行、修改工具逐项批准/拒绝及取消；用模拟 API 测试拒绝不会执行 Office.js 操作。
- [x] 5.3 完成 Excel 功能区入口、清单权限、Content Security Policy 和本地图标；运行清单验证命令。

## 6. 文档与可重复运行

- [x] 6.1 编写中文 README、架构说明、安装证书、启动、侧载、注销、移除证书和故障排查命令；复查中文编码和命令路径。
- [x] 6.2 提供一键启动和停止脚本，并验证重复运行不会创建冲突监听器或泄露配置内容。

## 7. 完整验证与归档

- [x] 7.1 运行 `npm test`、`npm run check`、`npm run validate:manifest`、`npm audit --omit=dev` 和中文乱码检查，修复所有项目内失败。
- [x] 7.2 启动服务并验证 HTTPS 健康、脱敏配置、端口所有者和非授权 Origin 拒绝行为。
- [ ] 7.3 在 Microsoft Excel 中完成真实侧载，验证只读、写值、公式、格式、表格和图表流程，并检查最终任务窗格视觉状态。
- [ ] 7.4 运行严格 OpenSpec 验证，把增量规格同步到主规格并归档变更，确认任务和规格无漂移。

## 8. ChatExcel 任务窗格重构

- [x] 8.1 把加载项、功能区和任务窗格名称改为 ChatExcel / ChatEx；重排为操作记录在上、对话在下，并显示“文件名-活动工作表”标签。
- [x] 8.2 实现毛玻璃视觉、紧凑操作记录、底部模型/思考等级/上下文控制和默认单行自增长输入框；在窄任务窗格与宽视口验证不溢出。
- [x] 8.3 实现图片添加、压缩、预览、删除和服务端类型/数量/大小验证，验证图片以 Responses `input_image` 传递且不写入磁盘。

## 9. 配置与模型控制

- [x] 9.1 实现进程内运行时配置：默认使用系统 Codex 配置，关闭后支持自定义 API URL、API Key、模型 ID、上下文长度和思考等级，且不回传或持久化密钥。
- [x] 9.2 实现模型发现接口和思考等级映射；设置页只允许选择结构化发现到的模型 ID，并把选择应用到下一条消息及后续工具步骤。
- [x] 9.3 把提供方 usage 映射为上下文占用并显示在输入区；usage 不存在时显示未知状态。

## 10. 审批模式与历史保护

- [x] 10.1 在输入区右下角实现颜色区分的“需要审批 / 无需审批”模式；逐项审批保持失败关闭，无需审批先告知再执行。
- [x] 10.2 让操作记录行可进入历史上下文；发送新对话前必须确认，取消后保持历史视图。
- [x] 10.3 订阅工作表手动变更；历史视图中确认则保留修改并回到最新上下文，取消则尝试原生撤销，Agent 自身修改不得重复提示。

## 11. 扩展验证与归档

- [x] 11.1 补充运行时配置、模型发现、图片输入、上下文 usage、审批模式和历史状态测试，运行全部项目门禁和中文乱码检查。
- [ ] 11.2 在浏览器与 Microsoft Excel 中验证主要交互和视觉状态，完成 `design-qa.md`、验证记录和截图证据。
- [ ] 11.3 同步全部主规格，运行严格 OpenSpec 验证，完成 100% 任务后归档变更并再次验证。

## 12. 多协议、持久化与粘贴附件

- [x] 12.1 更新 proposal、design、tasks 和相关增量规格，移除“仅 Responses、仅进程内配置”的旧约束；运行严格 OpenSpec 校验。
- [x] 12.2 实现 API 根地址规范化、四种生成端点和按协议模型发现；用模拟提供方验证 URL、认证头、文本、图片、工具定义、工具调用、工具结果和 usage 归一化。
- [x] 12.3 实现 `%APPDATA%\ChatExcel\settings.json` 持久化与 Windows 当前用户 DPAPI 密钥保护；验证明文不写盘、不回传，并验证重启与系统/自定义模式切换后配置恢复。
- [x] 12.4 在设置页加入协议下拉框和根地址说明，在输入框加入剪贴板图片处理；复用现有压缩、数量、类型、大小、预览和删除逻辑。
- [x] 12.5 按 `prefers-reduced-motion` 增加设置页切换、附件进入、状态反馈和按钮响应的克制微交互；验证键盘、焦点、CJK 组合输入和窄窗格不回归。
- [x] 12.6 完成浏览器交互、同视口视觉对比、`design-qa.md` 和真实截图；运行全部测试、语法检查、严格 OpenSpec 验证与中文乱码检查。
- [x] 12.7 把最大步骤数放入设置页，默认 100、限制为 1 到 1000 并长期保存；验证默认值、范围错误、重启恢复和实际上限错误文本。

## 13. 双语文档与 GitHub 发布

- [x] 13.1 以英文 `README.md` 作为默认入口，新增完整 `README.zh-CN.md`，自然介绍问题、产品、能力、场景、工作原理、技术栈、安装、配置、安全、验证和限制。
- [x] 13.2 把真实任务窗格与设置页截图放入仓库资源目录，并在两份 README 中按对应功能引用和说明。
- [ ] 13.3 检查本机 GitHub CLI 登录和现有远程配置；在不覆盖其他仓库的前提下创建 ChatExcel 远程仓库、提交并推送源码与文档。
