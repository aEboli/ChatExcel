## 1. 共享策略与工具契约

- [x] 1.1 在共享 Excel 工具模块定义范围型修改和自动调整的影响限制、图表有限位置地址规则及对应结构化错误码。
- [x] 1.2 为共享工具 Schema 补充整行整列自动调整和图表位置边界的回归测试。

## 2. Office.js 执行与验证

- [x] 2.1 在 Office.js 执行器中为范围型修改加入实际尺寸预检、影响摘要和自动调整维度限制。
- [x] 2.2 为值、公式、格式、数字格式、清除、排序、表格和图表返回操作特定的写后验证摘要。
- [x] 2.3 扩展模拟 Office.js 测试，验证超限不会修改、整列自动调整兼容、写后读回和验证失败语义。

## 3. 原生 XLS 对齐

- [x] 3.1 在原生 `.xls` 执行器加入相同的范围/自动调整限制和影响摘要。
- [x] 3.2 为原生范围操作返回核心验证字段，并通过 Launcher 构建和原生 smoke 验证兼容性。

## 4. Agent 质量规则与回归

- [x] 4.1 更新 Agent 工作簿质量指令，要求公式优先、先读后改和完成前处理验证摘要，同时不改变审批模式。
- [x] 4.2 补充审批模式回归测试，证明 `required` 仍逐项确认、`auto` 仍告知后直接执行。

## 5. 验证与规格同步

- [x] 5.1 运行相关 Node 测试、`npm run check`、Launcher 构建和严格 OpenSpec 验证。
- [x] 5.2 复核 proposal、design、增量规格和 tasks 与最终实现一致，并记录本轮真实 Excel `.xls` 宿主验收结果。
