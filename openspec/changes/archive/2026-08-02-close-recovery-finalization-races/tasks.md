## 1. 会话收尾一致性

- [x] 1.1 为挂起、普通取消和严格恢复清除实现按会话 ID 的收尾登记、等待和身份安全清理。
- [x] 1.2 为挂起与两种清除的受控并发、会话 ID 复用以及恢复/心跳等待收尾添加 `SessionManager` 回归测试。

## 2. 工作簿绑定迁移

- [x] 2.1 将后续消息的 `workbookBinding` 传入并在 `SessionManager` 中验证、迁移恢复绑定。
- [x] 2.2 为绑定缺失失败关闭、另存为迁移和 HTTP 选项转发添加聚焦测试。

## 3. 恢复锁协议

- [x] 3.1 以完整候选目录和 token 专属 owner 文件替换硬链接 `.reclaim` 锁释放协议，并兼容安全回收陈旧旧格式锁。
- [x] 3.2 为延迟释放、并发陈旧回收、空旧锁和旧文件锁迁移添加受控跨实例测试。

## 4. 验证与归档

- [x] 4.1 运行受影响 Node.js 测试、`npm test`、`npm run check`、清单验证和两个 Release 构建。
- [x] 4.2 运行严格 OpenSpec 验证，合并增量规格、归档本变更并复核工作区差异。
