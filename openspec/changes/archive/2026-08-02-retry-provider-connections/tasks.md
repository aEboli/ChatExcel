## 1. 传输完整性与提供方重连

- [x] 1.1 标记 SSE 响应的正常终止与读取中断，确保截断流被识别为可恢复传输错误。
- [x] 1.2 在 ProviderClient 中实现总超时内的 3 秒固定等待和最多 10 次可中止重连，保留非传输错误的立即失败行为。
- [x] 1.3 为流式尝试发出仅撤销本次文字后缀的草稿重置与无敏感信息的重连状态事件，且不改变本机 SSE 客户端断开后的会话取消。

## 2. 任务窗格反馈

- [x] 2.1 处理流式草稿后缀重置和重连状态，确保成功重连后的文字不会与中断尝试的文字混合显示，也不删除较早模型步骤的文字。

## 3. 验证

- [x] 3.1 补充提供方客户端与流式解析器测试：重连成功、十次耗尽、等待期间停止、HTTP 终态错误和中途流断连。
- [x] 3.2 补充任务运行器/历史状态测试，验证重连事件透传、草稿后缀撤销和多步骤文字保留。
- [x] 3.3 运行 `npm run check`、相关单元测试、`npm test`、`openspec validate retry-provider-connections --strict`，并复核中文文本。

## 4. 审查回归修正

- [x] 4.1 修正协议结构错误分类、半帧 EOF、默认 SSE 错误、空结构、重连耗尽时的草稿回滚、自动恢复步骤边界与多步骤终态文本保留，并补充回归测试。
