## 1. 本地角色资源

- [x] 1.1 将用户提供的六张人物图加入受版本控制的本地资源目录，并确认文件完整可读。

## 2. 页脚场景实现

- [x] 2.1 用六个对应的本地图片元素替换 CSS 几何行走角色，同时保留页脚结构与无障碍属性。
- [x] 2.2 调整角色裁切、层级和动画容器，使其适配 28px 页脚并保留 reduced-motion 静态位置。

## 3. 验证

- [x] 3.1 扩展任务窗格布局测试，验证六张本地角色资源和离线本地路径。
- [x] 3.2 运行 `npm run check`、聚焦的任务窗格布局测试与 `openspec validate use-provided-easter-characters --strict`。
