## 1. 步骤快照

- [x] 1.1 在工具执行完成和下一工具开始之间捕获范围或图表预览，并确保预览不进入模型工具结果。
- [x] 1.2 为 Excel 图像捕获设置有限范围和安全回退，为原生 `.xls` 提供只读范围数据回退。

## 2. 历史界面

- [x] 2.1 让任意操作记录显示对应的内存预览，最新步骤不触发历史锁，并在清除或恢复展示状态时释放预览。
- [x] 2.2 增加窄任务窗格可用的预览布局和明确的“不会修改或保存工作簿”提示。

## 3. 验证

- [x] 3.1 补充 AgentRunner、预览模块和任务窗格契约测试，验证预览失败不影响工具结果且数据不持久化。
- [x] 3.2 运行 `npm test`、`npm run check`、`openspec validate visual-history-preview --strict`，并记录无法在当前会话完成的真实 Excel 手动验收。

## 手动验收缺口

- 当前会话已完成任务窗格演示页的窄窗格与桌面宽度交互检查，但没有可操作的真实 Excel 主机。仍需在 Excel 中依次执行范围写入、格式化、创建表格、排序和创建图表，点击每项操作记录确认快照对应当时状态，并确认工作簿未变脏、未触发保存或自动保存。
