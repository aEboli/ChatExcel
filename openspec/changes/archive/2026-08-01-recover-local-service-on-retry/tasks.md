## 1. 服务守护

- [x] 1.1 实现项目范围的后台服务守护器，复用现有 HTTPS 健康检查、PID 与端口所有权边界，并加入有界恢复节奏。
- [x] 1.2 调整启动和停止脚本：启动时确保单个守护器，显式停止时先禁用守护再清理服务。

## 2. 发行与说明

- [x] 2.1 更新启动器资源检查和中英文运行说明，使开发与发行流程均携带服务恢复脚本。

## 3. 验证

- [x] 3.1 补充服务生命周期覆盖，验证受管 Node 服务被终止后会恢复、端口所有权不被越界处理、显式停止不会重启。
- [x] 3.2 运行 JavaScript 检查、项目测试、Launcher 构建、严格 OpenSpec 验证和实际本地服务恢复冒烟测试。
