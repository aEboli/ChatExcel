## 1. 发行准备

- [x] 1.1 归档 `release-0-0-1` 并将其增量规格同步到主规格。
- [x] 1.2 将 npm、服务、Launcher 和发行脚本版本统一为 `0.0.2`，并避免发行脚本保留重复的硬编码版本。
- [x] 1.3 更新中英文 README、更新日志、下载入口和 `.xls` / 本地恢复的边界说明；忽略原生冒烟构建输出。

## 2. 构建与验证

- [x] 2.1 运行 JavaScript 检查、全量 Node 测试、加载项清单校验和依赖审计。
- [x] 2.2 构建 Launcher，运行发行诊断与原生桥冒烟检查，并生成 `0.0.2` ZIP 和 SHA-256 资产。
- [x] 2.3 运行严格 OpenSpec 校验、版本一致性检查、发行资产内容/哈希检查和 `git diff --check`。

## 3. GitHub 发布

- [x] 3.1 审阅暂存内容，提交并推送当前 `main`，创建并推送 `v0.0.2` 标签。
- [x] 3.2 使用更新日志创建 GitHub Release，上传 ZIP 与 SHA-256，读取远端标签、Release 和资产复核结果。
