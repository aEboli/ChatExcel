## 1. 紧凑布局实现

- [x] 1.1 收紧任务窗格外层、顶部栏、操作记录、消息、composer、固定页脚和设置页的垂直留白，同时保持正文至少 12px/17px、常用控件至少 24px 高。
- [x] 1.2 更新 `tests/taskpane-layout.test.js` 的密度断言，覆盖关键固定区尺寸、正文可读下限和不使用全局缩放。

## 2. 验证与验收

- [x] 2.1 运行 `npm run check`、`npm test` 和 `npm run validate:manifest`，确认代码、全量测试与清单校验通过。
- [x] 2.2 在 400x900 和 320x700 浏览器视口验证无横向溢出、文字/控件不重叠、常用命中区域至少 24px，并更新截图与 `design-qa.md`。
- [x] 2.3 运行严格 OpenSpec 校验和中文乱码检查，确认规格、设计、任务与实现一致。
