## 1. 执行边界

- [x] 1.1 为数字格式定义并公开 5,000 单元格执行上限，在创建二维矩阵前以实际范围元数据失败关闭。
- [x] 1.2 更新 `set_number_format` 工具说明，使模型在请求前知道安全上限和分块恢复方式。

## 2. 验证

- [x] 2.1 扩展 Office.js 执行器测试，覆盖上限内一次赋值和超大整列/整行范围在赋值前被拒绝。
- [x] 2.2 运行 JavaScript 检查、项目测试和严格 OpenSpec 验证。
