## 1. 服务端能力边界

- [x] 1.1 先增加运行时配置与协议测试，覆盖仅返回模型 ID、Qwen3.7 的 Chat/Responses 协议分支、提供方已声明等级、自动默认和兼容集合外拒绝。
- [x] 1.2 实现 Qwen3.7 协议专有能力、已证实 `reasoningEfforts` 与 `compatibleReasoningEfforts` 的分离、公开状态传递、持久化恢复和逐模型会话校验；运行定向测试。

## 2. 请求传输语义

- [x] 2.1 先增加请求构造测试，覆盖自动省略、显式兼容档位、普通 `none`、Qwen3.7 `enable_thinking` 和 Responses 官方档位，并确认 DeepSeek V4 专有控制不回归。
- [x] 2.2 实现 OpenAI Responses / Chat Completions 的自动、显式档位、Qwen3.7 协议专有关闭和普通关闭传输语义；运行提供方客户端定向测试。

## 3. 任务窗格选择器

- [x] 3.1 先增加可执行的模型选择辅助逻辑测试，覆盖能力字段规范化、档位唯一名称、模型默认优先、自动回退和刷新后失效选择校正。
- [x] 3.2 实现可测试的模型选择辅助模块，并在模型点击、配置加载、模型重新发现和重置路径统一使用。
- [x] 3.3 在任务窗格展示自动与兼容档位，保持设置页只读并更新静态布局契约测试；运行任务窗格定向测试。

## 4. 验证与收尾

- [x] 4.1 运行 JavaScript 语法检查、全部 Node 测试、清单验证和严格 OpenSpec 校验，复查新增中文内容编码。
- [x] 4.2 使用隔离端口启动当前源码，完成宽窄视口的浏览器视觉与交互验证，确认当前 Qwen Chat 自动/关闭、未知模型兼容档位及 xhigh/max 文案无重叠或失效状态。
- [x] 4.3 将增量规格同步到主规格，确认任务全部完成后归档变更，并再次运行严格 OpenSpec 校验。
