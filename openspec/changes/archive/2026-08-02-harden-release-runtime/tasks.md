## 1. 本地运行时与发行链路

- [x] 1.1 扩展健康响应为版本化服务握手，并为版本/能力不匹配补充服务端测试。
- [x] 1.2 收紧启动、守护和停止脚本的回环监听、PID 启动时间、命令归属和显式停止校验，并增加可复核测试或静态验证。
- [x] 1.3 让 Launcher 构建严格检查依赖/发布退出码并从 `package.json` 注入程序集版本，验证发行元数据一致性。

## 2. 恢复快照与任务窗格真实状态

- [x] 2.1 为恢复快照的保存、心跳、清除和过期清理实现跨进程排他协议，并添加“清理旧文件时并发保存新文件”的回归测试。
- [x] 2.2 修复任务窗格在本地 SSE 传输失败、清除失败和工作簿标识变化时的恢复会话处理，并添加聚焦测试。
- [x] 2.3 让历史预览仅基于成功工具输出创建，并覆盖拒绝/失败矩阵写入场景。

## 3. 提供方完整性与凭据保护

- [x] 3.1 统一配置、模型发现和生成路径的认证信息脱敏器，并为 JSON、Basic 和 Bearer 错误正文添加回归测试。
- [x] 3.2 为 Anthropic、Gemini 和流式终态增加严格响应校验，并覆盖畸形 HTTP 200 响应。
- [x] 3.3 保留 Anthropic 流式签名和 redacted thinking，并用两轮工具续传测试验证请求完整性。

## 4. 原生 XLS 宿主边界

- [x] 4.1 将选区、事件和 Undo 限定到绑定工作簿，并扩展原生 smoke 覆盖跨工作簿场景。
- [x] 4.2 修复原生数字格式尺寸/混合读取和表格回滚，验证失败不会修改源数据。
- [x] 4.3 修复 Excel 打开失败、取消关闭和 WebView2 初始化的生命周期与退出码；使 native smoke 在临时副本上运行并核验原文件哈希。

## 5. 集成验证与规格闭环

- [x] 5.1 运行 Node 定向测试、`npm run check`、`npm test`、清单验证以及两个 Release .NET 构建。
- [x] 5.2 运行严格 OpenSpec 校验、变更差异检查和发行脚本的非破坏性验证；记录需要真实 Excel 或用户授权的运行时验收项。
