## ADDED Requirements

### Requirement: 快照读写和过期删除跨进程序列化
恢复快照的保存、心跳、显式清除和独立清理进程的过期删除 MUST 使用同一跨进程排他协议。持锁操作 MUST 在删除前重新读取当前加密快照并验证当前会话或过期条件；无法取得锁时 MUST 不执行未经验证的删除。

#### Scenario: 清理进程遇到并发新快照
- **WHEN** 清理进程已读取一个过期快照，同时服务保存同一工作簿的新快照
- **THEN** 清理进程不得删除新快照，后续恢复请求返回新的会话数据

#### Scenario: 进程异常遗留恢复锁
- **WHEN** 保存或清理进程异常退出并留下恢复锁
- **THEN** 后续操作只在锁被证明失效后回收它；在无法安全恢复锁时返回不含内容的恢复不可用状态，不删除快照

### Requirement: 任务窗格恢复状态必须保留可恢复会话
任务窗格 MUST 在本地 SSE 或同源服务传输异常后保留已知恢复 session ID，并在下一次可用操作前尝试恢复或继续该会话；只有服务确认匹配快照已删除后才清空本地恢复状态。每次恢复前 MUST 重新获取稳定工作簿绑定；Office 工作簿保存或另存为后使用其最新稳定标识，原生 `.xls` 没有跨启动稳定标识时 MUST 禁用恢复而不得使用随机管道 ID。

#### Scenario: 本地 SSE 在服务挂起会话后断开
- **WHEN** 本地事件流因网络或服务中断失败，而服务已将会话标记为可恢复
- **THEN** 任务窗格保留 session ID，后续用户继续操作时恢复该会话而不是创建并覆盖新的单快照

#### Scenario: 清除请求暂时失败
- **WHEN** 用户请求清除恢复会话但服务端删除请求失败
- **THEN** 任务窗格保留 session ID 和可重试状态，不显示已成功清除，也不将仍存在的快照遗忘

#### Scenario: 工作簿另存为后继续对话
- **WHEN** Office 工作簿在任务窗格打开期间被保存或另存为
- **THEN** 后续恢复和心跳使用新的稳定工作簿标识，不把副本会话写入旧文件的恢复键
