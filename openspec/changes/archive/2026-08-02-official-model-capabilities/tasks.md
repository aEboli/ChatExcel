## 1. 官方能力目录

- [x] 1.1 新增带官方来源的本地模型能力目录，覆盖 Qwen3.7 Max 和各协议的常用官方模型。
- [x] 1.2 将目录解析接入模型发现、当前模型补全和持久化配置恢复，并保留官方、上游和推断来源。
- [x] 1.3 让运行时按实际选中模型解析上下文和默认思考等级，阻止旧客户端覆盖只读思考等级。

## 2. 设置页行为

- [x] 2.1 在模型选择变化及模型刷新后回填目录上下文，同时保留上下文输入框的手动编辑能力。
- [x] 2.2 将设置页思考等级改为只读展示，并停止将其作为用户可配置项保存。

## 3. 验证

- [x] 3.1 增加服务端测试，覆盖官方目录优先、未知模型降级、持久化恢复、伪造思考等级和临时模型切换上下文。
- [x] 3.2 增加任务窗格静态行为测试，覆盖上下文回填、只读思考等级和保存请求边界。
- [x] 3.3 运行 JavaScript 检查、完整测试套件和严格 OpenSpec 变更验证，并复核本次涉及文件的差异；保留既有脏工作区差异。
