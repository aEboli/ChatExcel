## 1. 加密恢复存储

- [x] 1.1 新增独立的 `ConversationRecoveryStore`，使用当前 Windows 用户 DPAPI、原子写入、单会话覆盖和 30 分钟滚动 TTL。
- [x] 1.2 为恢复存储添加加密信封、过期、心跳、显式清除、损坏数据和并发写入测试。

## 2. 会话恢复语义

- [x] 2.1 将恢复检查点、工作簿绑定、显式取消与异常断开区分接入 `SessionManager`。
- [x] 2.2 实现安全会话重建：请求中断不重发，待工具调用转换为中断结果且不执行 Excel 操作。
- [x] 2.3 为服务重启、提供方终态失败、TTL 和未完成工具调用的恢复行为添加 `SessionManager` 测试。

## 3. 受限本地 API

- [x] 3.1 在服务启动时创建恢复存储，并为恢复、心跳和清除提供受同源保护且不可缓存的 HTTP 端点。
- [x] 3.2 将 SSE 客户端异常断开改为保留快照的挂起，并保留用户显式停止的删除语义。
- [x] 3.3 为恢复 API 的来源限制、脱敏响应、心跳和断开语义添加 HTTP 测试。

## 4. 任务窗格恢复体验

- [x] 4.1 在工作簿标识就绪后恢复当前对话展示和 session ID，并用心跳保持活动会话。
- [x] 4.2 在恢复状态中明确提示用户：不会自动重发模型请求或 Excel 写入，并提供显式清空当前恢复会话的控制。
- [x] 4.3 调整 `AgentRunner` 与 `HistoryState` 的恢复/清空行为，并添加聚焦单元测试。

## 5. 验证

- [x] 5.1 运行 `npm run check` 和全部新增及受影响的 Node.js 测试。
- [x] 5.2 运行 `openspec validate recover-local-conversation-context --strict`，并人工核查恢复文件不含明文提示词或工作簿值。
- [x] 5.3 根据最终生命周期审阅补充取消竞态、服务关闭、稳定工作簿绑定和清理进程启动失败的回归测试与规格。
- [x] 5.4 验证任务窗格持续打开但用户未聊天超过 30 分钟时恢复会话仍保留，以及最后一次成功任务窗格存活心跳后 30 分钟会删除恢复会话的回归行为。
