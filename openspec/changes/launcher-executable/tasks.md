## 1. 规格与启动器

- [x] 1.1 增加一键启动、证书检查、服务复用、Excel 侧载和错误边界规格，并通过严格 OpenSpec 校验。
- [x] 1.2 实现自包含 Windows x64 Launcher，支持开发仓库目录和发行目录 `app` 定位。
- [x] 1.3 实现 `--diagnose` 无副作用检查并避免把敏感配置写入日志。

## 2. 打包与文档

- [x] 2.1 生成包含最小依赖和 Node 运行时的 `ChatExcel Launcher` 发行目录。
- [x] 2.2 更新中英文 README、安装/故障排查说明和发行目录结构。

## 3. 验证

- [x] 3.1 运行 JavaScript、单元测试、manifest、依赖审计和 OpenSpec 严格校验。
- [x] 3.2 在无副作用诊断模式验证开发目录与发行目录，确认正常启动路径复用现有服务与侧载脚本。
- [x] 3.3 在真实桌面 Excel 中双击 EXE 完成一次加载项打开冒烟验收。
