## 1. OpenSpec 与传输层

- [x] 1.1 增加多协议 SSE、JSON 回退、取消和错误处理规格，并运行严格 OpenSpec 校验。
- [x] 1.2 实现可复用 SSE 解析器与四种协议的增量累积器，补充逐协议单元测试。

## 2. 服务端会话流

- [x] 2.1 让 Provider Client 在流式请求中发出脱敏文字增量，并保持最终响应归一化。
- [x] 2.2 让 SessionManager 和 HTTP API 绑定当前请求回调、发送 `result/done` 事件并处理中途断开。
- [x] 2.3 验证四种协议的请求字段、Gemini 流式端点、工具调用拼接和取消行为。

## 3. 任务窗格体验

- [x] 3.1 让 API 客户端和 AgentRunner 解析 SSE，并实时更新助手消息。
- [x] 3.2 将操作记录按一次任务分组，默认折叠显示任务摘要和步骤名，保留历史上下文选择。
- [x] 3.3 合并发送/停止按钮，加入旋转圆环、悬停停止态、焦点语义和 reduced-motion 回退，移除“模型处理中”。

## 4. 验证

- [x] 4.1 运行 `npm run check`、`npm test`、`npm run validate:manifest` 和中文乱码检查。
- [x] 4.2 在预览页验证流式文字、操作组展开/折叠、停止按钮和窄窗格布局，更新 `design-qa.md`。

## 尚未完成的外部验收

- [ ] 4.3 在真实桌面 Excel 中使用可控 SSE 提供方完成一次长文本流式输出和中途停止验收。
